-
  feature: 加密模块
  story: RSA加密接口
  title: 验证正常返回RSA加密接口数据
  request:
    method: POST
    url: http://101.34.221.219:5000/rsalogin
    data:
      username: ${rsa_encode(admin)}
      password: ${rsa_encode(123)}
  validate: null

